name: Tests

on:
  push:
    paths:
      - "**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
#        run: docker run --rm -v $(pwd):/app composer install
        run: ls -l
